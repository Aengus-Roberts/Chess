#pragma once

class Board {
private:
public:
    // Defines which players turn it is: white (0), black(1)
    int player;

    // states to check whether a player is in check
    int whiteCheck;
    int blackCheck;

    // Constructor
    Board();

    // Destructor
    ~Board();

    void U
};
